/* Attractive Gradient Background */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: linear-gradient(to right, white,white);
    padding-top: 80px; /* Ensures content is visible below the fixed header */
}

/* Fixed Header */
header {
    background-color: white;
    color:black;
    padding: 15px 20px;
    text-align: center;
    position: fixed;  
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
}

/* Navigation */
nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
}

nav ul li {
    display: inline;
    margin: 0 15px;
}

nav ul li a {
    color: black;
    text-decoration: none;
    font-weight: bold;
    transition: color 0.3s;
}

nav ul li a:hover {
    color:antiquewhite;
}

/* Enable Smooth Scrolling */
html {
    scroll-behavior: smooth;
}

/* Section Styles */
section {
    padding: 80px 20px;  /* Added padding so content is visible */
    text-align: center;
}

#hero {
    padding: 80px;
 background-image:url(shop.jpg);
    color: black;
}

.btn {
    display: inline-block;
    padding: 12px 24px;
    background: white;
    color: black;
    text-decoration: none;
    font-weight: bold;
    border-radius: 5px;
    transition: background 0.3s, transform 0.2s;
}

.btn:hover {
    background: white;
    transform: scale(1.1);
}

/* Products Section */
#products {
    text-align: center;
}

/* 6 Products in Grid */
.product-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    justify-content: center;
    padding: 20px;
}

.product-card {
    background: white;
    padding: 15px;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    text-align: center;
    transition: transform 0.3s, box-shadow 0.3s;
}

.product-card:hover {
    transform: scale(1.05);
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
}

.product-card img {
    width: 100%;
    height: auto;
    border-radius: 10px;
}

/* Buttons */
button {
    background:whitesmoke;
    color:black;
    padding: 12px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    transition: background 0.3s, transform 0.2s;
}

button:hover {
    background:#fff3e0;
    transform: scale(1.1);
}

/* Testimonials */
#testimonials {
    background: #fff3e0;
    padding: 50px;
    text-align: center;
}

.testimonial {
    max-width: 600px;
    margin: 20px auto;
    font-style: italic;
    font-size: 18px;
}

/* About & Contact */
#about, #contact {
    padding: 80px;
    text-align: center;
}

/* Footer */
footer {
    background-color: #333;
    color: white;
    text-align: center;
    padding: 15px;
}
